spring:
  application:
    name:
 profiles:
   active:
  datasource:
    driver-class-name:
    url:
    username:
    password:
  jpa:
    hibernate:
      ddl-auto:
    defer-datasource-initialization:
  sql:
    init:
      mode:
  jackson:
    property-naming-strategy:

app:
  jwt:
    secret:
    expiration:

server:
  port: